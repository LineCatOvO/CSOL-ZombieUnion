function UI.Event.OnSignal(signal)
    if signal==Show1KillIconSignal then

    elseif signal==Show2KillIconSignal then

    elseif signal==Show3KillIconSignal then
    
    elseif signal==Show4KillIconSignal then
    
    end
end